## 1. 言語判定基盤(Rust)

- [x] 1.1 `sys-locale` クレート(または同等の軽量クレート)を `app/src-tauri/Cargo.toml` に追加する
- [x] 1.2 `app/src-tauri/src/i18n.rs` を新設し、`Lang`(`Ja`/`En`)列挙体と、判定関数 `resolve_lang() -> Lang`(優先順位: `BWQA_LANG` 環境変数 → OSシステムロケール → フォールバック `En`)を実装する。判定ロジックの単体テスト(環境変数あり/なし、対応外の値、ロケール取得失敗時のフォールバック)を追加する
- [x] 1.3 `main.rs` / `lib.rs` の起動処理で `resolve_lang()` を1回呼び出し、結果を `AppState` 等アプリ全体から参照できる場所に保持する
- [x] 1.4 フロントエンドから現在の表示言語を取得できる新規Tauriコマンド(例: `get_ui_locale`)を `commands.rs` に追加する

## 2. トレイメニューの文言辞書化

- [x] 2.1 `app/src-tauri/src/i18n.rs` に `Messages` 構造体(トレイメニューで使う全文言をフィールドとして持つ)と、`Lang` ごとの `const` インスタンス(`JA` / `EN`)を定義する
- [x] 2.2 `app/src-tauri/src/tray.rs` の `status_label()` およびメニュー項目生成箇所(ホットキー警告文言、自動起動トグル文言、バージョン表示、終了)を、1.3で保持した言語に基づき `Messages` 経由で文言を取得する形に置き換える(実装時にスコープを広げ、`hotkey.rs` のホットキー登録失敗メッセージも合わせてローカライズ済み)
- [x] 2.3 日本語・英語それぞれでトレイメニューを開き、全項目が意図した言語で表示されることを確認する(実機確認が必要 — ユーザー確認待ち)

## 3. ポップアップUIの辞書化(フロントエンド)

- [x] 3.1 `app/dist/i18n.js` を新設し、`MESSAGES = { ja: {...}, en: {...} }` 形式で辞書を定義する。キー名はRust側の `Messages` フィールド名と対応が分かる命名にする
- [x] 3.2 `app/dist/index.html` に `<script src="i18n.js"></script>` を `app.js` より前に追加する
- [x] 3.3 `index.html` 内の静的な文言(アンロック画面の見出し・プレースホルダ・ボタン、検索プレースホルダ、空の検索結果メッセージ)に `data-i18n` / `data-i18n-placeholder` 属性を付与する
- [x] 3.4 `app.js` の起動処理に、`invoke("get_ui_locale")` を呼び出して現在の表示言語を取得し、`data-i18n` / `data-i18n-placeholder` を持つ全要素を走査して文言を差し替える初期化処理を追加する
- [x] 3.5 `app.js` 内でハードコードされている動的メッセージ(コピー成功時のフィードバック「ユーザー名をコピーしました」等、失敗時のエラーメッセージ)を、3.1の辞書を参照する形に置き換える
- [x] 3.6 日本語・英語それぞれで検索ポップアップを開き、アンロック画面・検索・コピー操作フィードバックが意図した言語で表示されることを確認する(実機確認が必要 — ユーザー確認待ち)

## 4. ドキュメント更新

- [x] 4.1 README.md / README.ja.md の `## Out of scope` から「In-app localization」の記述を削除し、実態(日本語・英語対応、切り替えはOSロケールまたは開発者向け環境変数)に合わせて更新する

## 5. 動作確認・仕上げ

- [x] 5.1 macOSのシステム言語設定を日本語に切り替えた実機で、トレイメニュー・検索ポップアップの双方が日本語表示になることを確認する
- [x] 5.2 macOSのシステム言語設定を英語に切り替えた実機(またはBWQA_LANG=enでの `cargo run`)で、トレイメニュー・検索ポップアップの双方が英語表示になることを確認する
- [x] 5.3 `specs/ui-localization/spec.md` の各シナリオが満たされていることを確認する
