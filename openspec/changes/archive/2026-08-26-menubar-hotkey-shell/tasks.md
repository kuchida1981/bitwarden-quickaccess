## 1. トレイアイコン

- [x] 1.1 メニューバー常駐アイコン(ロック済み/アンロック済み/未接続の3状態分の画像アセット)を用意する
- [x] 1.2 Tauri tray APIでアイコンを起動時に表示する
- [x] 1.3 バックエンドのロック状態変化イベントを購読し、アイコン表示を更新する
- [x] 1.4 コンテキストメニュー(ロック状態表示、終了)を実装する
- [x] 1.5 終了選択時に `bw serve` 子プロセスを含めて確実にアプリを終了させる

## 2. グローバルホットキー

- [x] 2.1 `tauri-plugin-global-shortcut` を導入し `⇧⌘Space` を登録する
- [x] 2.2 ホットキー押下でポップアップウィンドウの表示/非表示をトグルする処理を実装する
- [x] 2.3 ホットキー登録失敗時にクラッシュせず起動を継続し、失敗をログに記録する
- [x] 2.4 macOSのアクセシビリティ/Input Monitoring権限が必要な場合の初回リクエスト導線を実装する(登録失敗時のエラーログにシステム設定への案内文言を含める形で対応。実機確認では権限ダイアログなしに登録成功した)

## 3. ポップアップウィンドウ(骨格)

- [x] 3.1 プレースホルダ内容のウィンドウを画面上部中央に表示する設定を実装する
- [x] 3.2 ウィンドウの `blur`(フォーカスロスト)イベントで自動的に非表示にする処理を実装する

## 4. ログイン時自動起動

- [x] 4.1 `tauri-plugin-autostart`(または同等のSMAppService連携)を導入する
- [x] 4.2 コンテキストメニューに自動起動オン/オフのトグル項目を追加する
- [x] 4.3 設定値を永続化し、アプリ再起動後も状態が保持されることを確認する(状態の一次情報源をmacOS側のログイン項目登録そのものとし、起動時に `is_enabled()` で読み出すためアプリ独自の永続化ストレージは不要)

## 5. テスト・確認

- [x] 5.1 ロック状態変化に応じたアイコン切り替えをローカルで動作確認する(実機でロック中(オレンジ)表示を確認済み)
- [x] 5.2 ホットキーでの表示/非表示トグルとフォーカスロスト時の自動クローズを動作確認する(ユーザーが実機で確認: `⇧⌘Space` の再押下でポップアップが閉じる、別ウィンドウクリックでも自動的に閉じることを確認済み)
- [ ] 5.3 自動起動オン/オフがmacOSのログイン項目設定に正しく反映されることを動作確認する(ユーザーの都合により保留。実装自体はコードレビューで確認し、動作確認は後日ユーザーが実施)
