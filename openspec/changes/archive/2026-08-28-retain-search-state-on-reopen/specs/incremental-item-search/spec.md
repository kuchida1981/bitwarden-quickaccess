## ADDED Requirements

### Requirement: 再表示時の検索状態の一定時間保持
システムは、ポップアップウィンドウが非表示になった時刻を記録し、非表示から30秒以内にアンロック済み状態で再表示された場合、検索ボックスの文字列と検索結果一覧を非表示直前の状態のまま保持しなければならない(SHALL)。この場合、検索ボックスのテキストは全選択状態にしなければならない(SHALL)。

#### Scenario: 30秒以内の再表示で検索状態が保持される
- **WHEN** アンロック済み状態で検索文字列を入力した後にポップアップウィンドウが非表示になり、30秒以内に再表示される
- **THEN** 検索ボックスには非表示直前の検索文字列が保持され、検索結果一覧も非表示直前の内容のまま表示される

#### Scenario: 保持された検索ボックスのテキストが全選択状態になる
- **WHEN** 30秒以内の再表示によって検索状態が保持される
- **THEN** 検索ボックスのテキストは全選択状態になっており、続けて文字を入力すればその文字だけで絞り込みが行われる

### Requirement: TTL超過時の検索状態リセット
システムは、ポップアップウィンドウが非表示になってから30秒を超えて再表示された場合、検索ボックスの文字列をクリアし、全アイテムを表示する状態にリセットしなければならない(SHALL)。

#### Scenario: 30秒を超えた再表示で検索状態がクリアされる
- **WHEN** アンロック済み状態で検索文字列を入力した後にポップアップウィンドウが非表示になり、30秒を超えてから再表示される
- **THEN** 検索ボックスは空になり、vault内の全アイテムの一覧が表示される

### Requirement: ロック・切断時の検索状態非保持
システムは、⌘Lによる明示的ロックが行われた場合、またはバックエンド切断によりエラー画面へ遷移する場合、TTL内であっても検索状態を保持してはならない(SHALL NOT)。

#### Scenario: TTL内でも明示的ロック後は検索状態が保持されない
- **WHEN** 検索文字列を入力した状態で⌘Lによる明示的ロックが行われ、30秒以内にアンロックして検索画面が再表示される
- **THEN** 検索ボックスは空になっており、非表示直前の検索文字列は復元されない

#### Scenario: TTL内でもバックエンド切断後は検索状態が保持されない
- **WHEN** 検索文字列を入力した状態でバックエンド接続が切断されエラー画面に遷移し、30秒以内に接続が復旧して検索画面が再表示される
- **THEN** 検索ボックスは空になっており、非表示直前の検索文字列は復元されない
