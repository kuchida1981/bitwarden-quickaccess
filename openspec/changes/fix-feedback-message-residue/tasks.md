## 1. フィードバック消去ロジックの実装

- [ ] 1.1 `app/dist/app.js` の `showFeedback()` に、既存の `hidePopupTimer`(700ms、ポップアップを隠すためのタイマー)とは独立した自動消去タイマー(2〜3秒)を追加し、経過後にフィードバック要素のテキスト・状態をクリアする。
- [ ] 1.2 ポップアップが非表示になる経路(`hide_popup` コマンド呼び出し、`app/src-tauri/src/popup.rs` の `Focused(false)` による自動hide)で、フロントエンド側のフィードバック状態を確実にリセットする。既存の `POPUP_SHOWN_EVENT` 購読・emit の仕組みに合わせて実装する(新規イベントを増やす場合は `popup.rs` の該当箇所と `app.js` の購読側を対応させる)。
- [ ] 1.3 1.1 のタイマーと 1.2 のリセットが競合しないこと(片方が発火してももう片方でエラーにならないこと)を確認する。

## 2. 動作確認

- [ ] 2.1 `cargo test` / 既存フロントエンドのビルド確認を実行し、既存テストが通ることを確認する。
- [ ] 2.2 実機(`cargo tauri dev` 等)で、コピー実行直後(700ms以内)に素早くクイックアクセスを閉じて再度開き、古いフィードバックメッセージが残っていないことを確認する。
- [ ] 2.3 通常のコピー成功時・失敗時のフィードバック表示自体が従来通り機能することを確認する(回帰確認)。
