## 1. プロジェクト基盤

- [x] 1.1 リポジトリ構成を決定する(`bin/bw-quickaccess` をエントリポイントとし、機能ごとに `lib/` 以下へ関数を分割する構成とする)
- [x] 1.2 エントリポイントスクリプトの雛形(shebang、`set -euo pipefail`、関数読み込み)を作成する
- [x] 1.3 キャッシュディレクトリ(`~/.cache/bw-quickaccess/`)の作成・パーミッション設定処理を実装する

## 2. environment-preflight(起動前チェック)

- [x] 2.1 `bw`/`jq`/`fzf` の存在確認と、不足時のインストール案内メッセージを実装する
- [x] 2.2 `fzf --version` を確認し、必要な `--bind` 構文(`execute-silent` 等)に対応する最低バージョンを満たさない場合にエラー終了させる処理を実装する
- [x] 2.3 OS/ディスプレイサーバー判定ロジック(`uname`、`$WAYLAND_DISPLAY`、`$DISPLAY`)を実装する
- [x] 2.4 判定結果に応じたクリップボードコマンド(`pbcopy`/`wl-copy`/`xclip`/`xsel`)の存在確認と、不足時のインストール案内を実装する
- [x] 2.5 keychain 連携コマンド(macOS: `security`、Linux: `secret-tool`)の存在確認と疎通確認(store/lookup の試行)を実装する
- [x] 2.6 Linux で keyring バックエンドへの疎通に失敗した場合、キャッシュ無効フォールバックの通知メッセージを実装する
- [x] 2.7 preflight チェック一式を起動時に呼び出す統合処理を実装する

## 3. bw-session-management(セッション管理)

- [x] 3.1 マスターパスワードを対話入力させて `bw unlock` を実行し、session token を取得する処理を実装する
- [x] 3.2 取得した session token を OS keychain へ保存する処理(macOS: `security add-generic-password`、Linux: `secret-tool store`)を実装する
- [x] 3.3 session token の発行時刻を `~/.cache/bw-quickaccess/session-issued-at` に記録する処理を実装する
- [x] 3.4 起動時に keychain から session token を読み出す処理を実装する
- [x] 3.5 発行時刻と TTL(既定値・環境変数での上書きに対応)を比較し、期限超過時は再認証させる処理を実装する
- [x] 3.6 `bw` コマンド実行時に session 無効エラーを検知し、その場で再認証にフォールバックする処理を実装する
- [x] 3.7 session token を環境変数 `BW_SESSION` 経由でのみ `bw` に渡し、コマンドライン引数に含めないことを徹底する
- [x] 3.8 キャッシュされた session を破棄する `bw-quickaccess lock` サブコマンドを実装する

## 4. vault-item-search(検索画面)

- [x] 4.1 `bw list items` の出力を `jq` で fzf 向けの表示行(アイテム名等)+ 内部 ID にマッピングする処理を実装する
- [x] 4.2 fzf によるインクリメンタルサーチ画面を起動し、選択結果からアイテム ID を取得する処理を実装する
- [x] 4.3 直前選択アイテム ID のキャッシュファイル(`~/.cache/bw-quickaccess/last-item-id`)の読み書き処理を実装する
- [x] 4.4 起動時にキャッシュがあれば検索画面をスキップしてフィールド選択画面に直接遷移する制御フローを実装する
- [x] 4.5 フィールド選択画面から Esc で検索画面に戻る際の制御ループを実装する
- [x] 4.6 アイテム決定時(検索・直前アイテム双方)にキャッシュを更新する処理を実装する

## 5. credential-clipboard-copy(フィールド選択・コピー)

- [x] 5.1 選択アイテムの詳細(`bw get item <id>`)を取得し、ユーザー名・パスワード・TOTP の有無を判定する処理を実装する
- [x] 5.2 フィールド選択画面(fzf)のリスト表示(値はマスクしラベルのみ表示)を実装する
- [x] 5.3 各フィールドのコピー処理を、値をコマンドライン引数に載せずクリップボードコマンドへ標準入力で渡す形で実装する
- [x] 5.4 存在しないフィールドを選択した場合のハンドリング(コピーせず通知)を実装する
- [x] 5.5 `--bind` によるキーバインドショートカット(例: ctrl-u→username, ctrl-p→password, ctrl-t→totp)を実装する
- [x] 5.6 コピー後も画面を終了させず同一アイテムの別フィールドを連続コピーできるよう `execute-silent` ベースの処理を実装する
- [x] 5.7 Esc/明示キーによるフィールド選択画面の終了処理を実装する
- [x] 5.8 標準出力・標準エラー出力に機密情報(パスワード・TOTP・ユーザー名の値)が一切出力されないことをコードレビューで確認する

## 6. 結合・検証

- [x] 6.1 検索画面 → フィールド選択画面 → 連続コピー → Esc → 検索画面、という一連の操作フローを手動で通しで検証する
- [x] 6.2 直前アイテムキャッシュを使った再起動時のスキップ動作を手動で検証する
- [x] 6.3 session TTL 超過時・`bw lock` 実行後の再認証フォールバックを手動で検証する
- [x] 6.4 必須コマンド不足時・クリップボードコマンド不足時のエラー終了メッセージを手動で検証する
- [x] 6.5 macOS と Linux(デスクトップ環境)双方で一連の動作を検証する
- [x] 6.6 README にセットアップ手順(依存ツールのインストール、`bw login` 前提条件、実行方法)を記載する
