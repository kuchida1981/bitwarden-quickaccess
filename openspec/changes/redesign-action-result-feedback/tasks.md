## 1. 点滅アニメーションの実装

- [x] 1.1 `app/dist/style.css` に、フォーカス行(`li.focused`)向けの点滅アニメーション用クラス(例: `.flash`)を追加する。既存の `#feedback` 関連スタイル(`ok`/`error`/`visible`)は削除する。
- [x] 1.2 `app/dist/app.js` に、指定した `<li>` 要素へ点滅クラスを付与し、アニメーション終了後(`animationend` またはアニメーション時間に合わせた `setTimeout`)に自動でクラスを外すヘルパー関数を実装する。

## 2. アクション結果ハンドリングの置き換え

- [x] 2.1 `runAction`(`app.js:345-363`)を、`showFeedback` 呼び出しと `hidePopupTimer` による700ms後の自動 `hide_popup` 呼び出しを廃止し、1.2 の点滅ヘルパーを呼び出す形に置き換える。
- [x] 2.2 点滅アニメーション完了後、アクションが成功していれば `invoke("hide_popup")` を呼ぶ。失敗していれば何も呼ばず、検索画面のフォーカス・検索結果をそのまま維持する。
- [x] 2.3 `showFeedback` 関数、`feedback` DOM参照、`FEEDBACK_DISPLAY_MS` 定数など、不要になったコードを削除する。
- [x] 2.4 `app/dist/index.html` から `#feedback` 要素を削除する。

## 3. 不要になったi18n文字列の削除

- [x] 3.1 `app/dist/i18n.js` から `copiedUsername` / `copiedPassword` / `copiedTotp` / `openedInBrowser` / `actionFailed` の日英エントリを削除する。
- [x] 3.2 `app.js` 内でこれらのキーを参照していた箇所(`executeItemAction`, `runAction`)が新しい実装で参照しなくなっていることを確認する。

## 4. 動作確認

- [x] 4.1 `cargo test` を実行し、既存テストが通ることを確認する。(2026-08-26 実行、全35テスト成功)
- [ ] 4.2 実機(`cargo tauri dev` 等)で、ユーザー名/パスワード/TOTPコピーが成功する場合に、フォーカス行が点滅した後にクイックアクセスが閉じることを確認する。
- [ ] 4.3 実機で、TOTP未設定アイテムに `⌥⌘C` を押した場合(失敗ケース)に、フォーカス行が点滅した後もクイックアクセスが閉じずに検索画面が維持されることを確認する。
- [ ] 4.4 実機で、URL未設定アイテムに `Enter` を押した場合も同様に、点滅後クイックアクセスが維持されることを確認する。
- [ ] 4.5 アクションメニュー(矢印キーで開く)経由でのアクション実行でも、同じ点滅+開閉挙動になることを確認する。
- [ ] 4.6 日本語・英語それぞれの表示言語で検索ポップアップの他の文言(アンロック画面・検索結果0件時など)が引き続き正しく表示されることを確認する(回帰確認)。
