## 1. プロジェクト雛形

- [ ] 1.1 `app/` に Tauri(Rust)プロジェクトの雛形を作成する(UIなし、最小限のバックエンドのみで起動できる状態)
- [ ] 1.2 `Cargo.toml` に `reqwest`(JSON機能付き)、`serde` / `serde_json`、非同期ランタイム(`tokio`)を依存として追加する
- [ ] 1.3 `app/README.md`(or 相当)に開発環境セットアップ手順(Rust toolchain, Tauri CLI)を最小限記載する

## 2. bw CLI 前提チェック

- [ ] 2.1 `bw` コマンドの存在確認と `serve` サブコマンド対応バージョンの確認を行う関数を実装する(参考: `lib/preflight.sh` の既存チェックパターン)
- [ ] 2.2 チェック失敗時にアプリ内部状態へエラーを反映する経路を実装する

## 3. bw serve プロセスライフサイクル管理

- [ ] 3.1 空きポートを動的に選択するロジックを実装する
- [ ] 3.2 `bw serve --hostname localhost --port <port>` を子プロセスとして起動する関数を実装する
- [ ] 3.3 子プロセスの異常終了を検知し、アプリ内部状態を「バックエンド未接続」に更新する監視ロジックを実装する
- [ ] 3.4 アプリ終了時に子プロセスを確実に終了させる終了処理を実装する

## 4. bw serve HTTP クライアントラッパー

- [ ] 4.1 `/status` を叩き、ロック状態を返す関数を実装する
- [ ] 4.2 `/unlock`(POST, パスワード)を叩き、成否を返す関数を実装する
- [ ] 4.3 `/lock`(POST)を叩く関数を実装する
- [ ] 4.4 `/list/object/items?search=...` を叩き、アイテム一覧を返す関数を実装する(ロック中のエラーレスポンスをそのまま呼び出し元に伝播させる)
- [ ] 4.5 `/object/item/{id}` を叩き、アイテム詳細(username/password/uris含む)を返す関数を実装する
- [ ] 4.6 `/object/totp/{id}` を叩き、TOTPコードを返す関数を実装する

## 5. 内部状態管理

- [ ] 5.1 ロック状態(未接続/ロック済み/アンロック済み)を保持するアプリ内部状態モジュールを実装する
- [ ] 5.2 アンロック成功時・ロック実行時・子プロセス異常終了時に内部状態が正しく更新されることを確認する

## 6. テスト

- [ ] 6.1 HTTPクライアントラッパー関数の単体テスト(モックサーバ or `bw serve` の実プロセスを使ったテスト用ヘルパー)を追加する
- [ ] 6.2 bw CLI前提チェックの単体テスト(コマンド未検出・バージョン不足のケース)を追加する
- [ ] 6.3 `cargo build` / `cargo test` がCI相当のローカル確認で通ることを確認する
