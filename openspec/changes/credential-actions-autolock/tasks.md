## 1. コピーアクション(Rustコア)

- [x] 1.1 `copy_field` Tauri command(itemId, field: username/password/totp)を実装し、`bw-serve-backend` から値を取得してクリップボードへ書き込む
- [x] 1.2 TOTP未設定など対象フィールドが存在しない場合のエラーハンドリングを実装する

## 2. ブラウザ起動アクション(Rustコア)

- [x] 2.1 `open_in_browser` Tauri command(itemId)を実装し、`login.uris[0].uri` をデフォルトブラウザで開く
- [x] 2.2 URL未設定時のエラーハンドリングを実装する

## 3. キーボードショートカット(フロントエンド)

- [x] 3.1 一覧行にフォーカスがある状態でのkeydownハンドラ(`⌘C`/`⌘⇧C`/`⌥⌘C`/`Enter`)を実装する
- [x] 3.2 検索ボックスにフォーカスがある間は `⌘C` を素通りさせ、独自アクションを発火させない分岐を実装する(検索ボックスにテキスト選択がある場合のみOS標準コピーに委ね、選択がなければ独自アクションを発火する方式で実装。design.mdでは「フォーカス対象で分岐」とあったが、本実装では検索ボックスへのDOM focusが常時維持される設計のため、テキスト選択の有無で判定する方式に調整した)
- [x] 3.3 各ショートカットから対応するTauri commandを呼び出す処理を実装する

## 4. フィードバック表示

- [x] 4.1 コピー成功/失敗、ブラウザ起動失敗のフィードバックUI(既存copy-feedbackの考え方を踏襲)を実装する
- [x] 4.2 アクション実行後にポップアップウィンドウを閉じる処理を実装する(成功/失敗を問わず、短いフィードバック表示の後に閉じる)

## 5. アイドル自動ロック(Rustコア)

- [x] 5.1 アイドルタイマー(既定15分)をRustコア内に実装する
- [x] 5.2 対象操作(unlock/search/copy/open browser)呼び出し時にタイマーをリセットする処理を実装する(get item/get totpは常にcopy_field/open_in_browser経由でのみ呼ばれるため、個別のリセットは不要)
- [x] 5.3 タイムアウト到達時に `/lock` を呼び出し、内部状態を更新する処理を実装する(30秒間隔のバックグラウンドタスクでチェック)

## 6. テスト・確認

- [ ] 6.1 各ショートカットでのコピー/ブラウザ起動をローカルで動作確認する(マスターパスワードの入力が必要なため、Claude Codeはアンロックを伴う動作確認を行わない方針。ユーザー確認待ち)
- [ ] 6.2 検索ボックスフォーカス時に `⌘C` が通常のテキストコピーとして機能することを確認する(同上、ユーザー確認待ち)
- [ ] 6.3 アイドルタイムアウトによる自動ロックと操作によるタイマーリセットを動作確認する(15分待つ実機確認が必要なため、ユーザー確認待ち。単体テストではタイマーの期限切れ・リセットロジック自体は検証済み)
