## 1. コピーアクション(Rustコア)

- [ ] 1.1 `copy_field` Tauri command(itemId, field: username/password/totp)を実装し、`bw-serve-backend` から値を取得してクリップボードへ書き込む
- [ ] 1.2 TOTP未設定など対象フィールドが存在しない場合のエラーハンドリングを実装する

## 2. ブラウザ起動アクション(Rustコア)

- [ ] 2.1 `open_in_browser` Tauri command(itemId)を実装し、`login.uris[0].uri` をデフォルトブラウザで開く
- [ ] 2.2 URL未設定時のエラーハンドリングを実装する

## 3. キーボードショートカット(フロントエンド)

- [ ] 3.1 一覧行にフォーカスがある状態でのkeydownハンドラ(`⌘C`/`⌘⇧C`/`⌥⌘C`/`Enter`)を実装する
- [ ] 3.2 検索ボックスにフォーカスがある間は `⌘C` を素通りさせ、独自アクションを発火させない分岐を実装する
- [ ] 3.3 各ショートカットから対応するTauri commandを呼び出す処理を実装する

## 4. フィードバック表示

- [ ] 4.1 コピー成功/失敗、ブラウザ起動失敗のフィードバックUI(既存copy-feedbackの考え方を踏襲)を実装する
- [ ] 4.2 アクション実行後にポップアップウィンドウを閉じる処理を実装する

## 5. アイドル自動ロック(Rustコア)

- [ ] 5.1 アイドルタイマー(既定15分)をRustコア内に実装する
- [ ] 5.2 対象操作(unlock/search/get item/get totp/copy/open browser)呼び出し時にタイマーをリセットする処理を実装する
- [ ] 5.3 タイムアウト到達時に `/lock` を呼び出し、内部状態を更新する処理を実装する

## 6. テスト・確認

- [ ] 6.1 各ショートカットでのコピー/ブラウザ起動をローカルで動作確認する
- [ ] 6.2 検索ボックスフォーカス時に `⌘C` が通常のテキストコピーとして機能することを確認する
- [ ] 6.3 アイドルタイムアウトによる自動ロックと操作によるタイマーリセットを動作確認する
